
# Set the logging levels on some packages
logging.level.root=info
logging.level.ch.fhnw.test.testspring=debug

# Disable Spring Banner on startup
spring.main.banner-mode=console

# MongoDB Properties
spring.data.mongodb.database=test-spring

# Set the context path under which URL the webapp is accessible
server.servlet.context-path=/test-spring